import { FlowPlaceholderPage } from '../store/FlowPlaceholderPage'

export function ProfileDeleteAccountPage() {
  return (
    <FlowPlaceholderPage
      title="Delete my Account"
      body="Account deletion is a regulated flow. This screen will confirm identity and wallet settlement before removal."
    />
  )
}
