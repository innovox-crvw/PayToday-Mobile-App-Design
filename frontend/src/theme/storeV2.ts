/** PayToday Store v2 layout guide — scoped tokens (do not replace global theme). */
export const SHOP_V2 = {
  pageBackground: '#F4F5F7',
  accent: '#5D2D91',
  success: '#2D915D',
  radius: '12px',
  /** Listing cards: single “% off” pill (retail style — not success green). */
  listingDiscountChip: {
    bg: '#E0F2FE',
    color: '#0369A1',
  },
} as const
