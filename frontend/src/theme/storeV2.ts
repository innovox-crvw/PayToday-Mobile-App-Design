/** PayToday Store v2 layout guide — scoped tokens (do not replace global theme). */
export const SHOP_V2 = {
  pageBackground: '#F4F5F7',
  accent: '#5D2D91',
  success: '#2D915D',
  radius: '12px',
} as const
