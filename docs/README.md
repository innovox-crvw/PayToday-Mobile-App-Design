# Documentation (`docs/`)

Start with the **[Project handbook](PROJECT_HANDBOOK.md)** for a full picture of the product: **customer-facing name** and theme tokens live in `frontend/src/theme/branding.ts` (today **AvoToday**), while the repository and API often use **PayToday Store** strings — the handbook explains that split, maps all major routes and `/api` prefixes, and links to deeper specs (Keycloak, payment intent, deploy).

## Quick links

| Document | Purpose |
|----------|---------|
| [PROJECT_HANDBOOK.md](PROJECT_HANDBOOK.md) | Product and platform overview + curated document map |
| [DEPLOY.md](DEPLOY.md) | Deployment and environment configuration |
| [KEYCLOAK_API.md](KEYCLOAK_API.md) | Keycloak HTTP API index and examples |

The handbook’s **§10 Document map** lists every markdown file in this directory with a one-line summary.
